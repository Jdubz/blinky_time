# Cppcheck suppressions for Arduino/embedded projects
# See: https://cppcheck.sourceforge.io/manual.html#suppressions

# ============================================
# Arduino framework patterns
# ============================================

# Arduino ISR callbacks and setup/loop (called by framework, not our code)
unusedFunction:*setup*
unusedFunction:*loop*
unusedFunction:*ISR*
unusedFunction:*onPDMdata*
unusedFunction:*callback*

# Arduino-specific includes
missingIncludeSystem

# Arduino flash memory macro (PROGMEM)
unknownMacro:blinky-things/generators/Fire.cpp

# Suppress branch limiting information messages (not actual errors)
normalCheckLevelMaxBranches

# ============================================
# Embedded C++ patterns (acceptable in this context)
# ============================================

# C-style casts are common and acceptable in embedded for type punning
cstyleCast

# memset on structs with floats is intentional for zero-initialization
memsetClassFloat

# Unused private functions may be conditionally compiled or for future use
unusedPrivateFunction

# Variables that "could be const" - often intentional for embedded flexibility
constVariablePointer
constVariable

# Explicit constructors - test utilities intentionally allow implicit conversion
noExplicitConstructor

# Scope reduction suggestions - embedded code often pre-declares for performance
variableScope

# Uninitialized members - embedded code often initializes in begin() not constructor
uninitMemberVar
uninitMemberVarPrivate

# ============================================
# Conditional compilation artifacts
# ============================================

# IMU code has conditions that are always true/false when IMU_ENABLED is not defined
knownConditionTrueFalse:*IMUHelper*

# ============================================
# ArduinoJson library patterns
# ============================================

# ArduinoJson uses operator| for default values (not bitwise operation)
# This is intentional library design, not a redundant bitmask check
badBitmaskCheck:blinky-things/inputs/SerialConsole.cpp

# ============================================
# Directories to skip
# ============================================

*:*/tests/*
*:*/archive/*
*:*/examples/*
